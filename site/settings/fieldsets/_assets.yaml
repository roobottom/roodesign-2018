fields:
  asset_sets:
    sets:
      set:
        display: Set
        fields:
          name:
            type: text
            display: Name
          assets:
            type: assets
            display: Assets
            container: main
          brickwall:
            type: toggle
            display: Brickwall effect (resize)?
            width: 50
          stage:
            type: toggle
            display: Put this figure on a stage?
            width: 50
          caption:
            cheatsheet: false
            restrict_assets: false
            type: markdown
            display: Caption
          width:
            options:
              default: Default
              wide: Wide
              x-wide: 'Extra Wide'
              aside: 'Aside (float right)'
            inline: false
            type: radio
            display: Overall width
            default: default
          width_individual:
            display: Individual Width
            type: text
            width: 50
            instructions: 'Set a manual `px` max-width for each image in this set (ignored by single images)'
          gap:
            display: Invidiual Gap
            type: text
            width: 50
            instructions: 'Set a manial `px` gap between each image in this set (ignored by single images)'
          style:
            type: checkboxes
            dislay: Style options
            options:
              shadow: Drop Shadow
              frame-browser: Browser Frame
              frame-phone: Phone Frame
              overlay: Blend with background
    type: replicator
    instructions: 'A grouping of assets you can refer to with a `{{ partial:figure set="name" }}` tag.'
    display: 'Asset Sets'
hide: true
